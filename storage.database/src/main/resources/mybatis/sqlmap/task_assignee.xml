<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.smart.framework.engine.persister.database.dao.TaskAssigneeDAO">

    <sql id="baseColumn">
        id, gmt_create, gmt_modified,process_instance_id,
        task_instance_id,assignee_id,assignee_type
    </sql>

    <insert id="insert" parameterType="TaskAssigneeEntity"  useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into se_task_assignee(<include refid="baseColumn"/>)
        values (#{id}, now(), now(), #{processInstanceId},
        #{taskInstanceId},#{assigneeId},#{assigneeType})
    </insert>

    <delete id="delete" parameterType="long">
        delete from se_task_assignee where id=#{id}
    </delete>

    <update id="update" parameterType="TaskAssigneeEntity">
        update se_task_assignee
        <set>
            gmt_modified=now()
            <if test="assigneeId != null">,assignee_id = #{assigneeId}</if>

        </set>
        where id=#{id}
    </update>

    <select id="findPendingTask" resultType="TaskAssigneeEntity">
        select
        <include refid="baseColumn"/>
        from se_task_assignee where assignee_id=#{assigneeId}
    </select>


</mapper>
