<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.smart.framework.engine.persister.database.dao.ExecutionInstanceDAO">
    <resultMap id="BaseResultMap"
               type="com.alibaba.smart.framework.engine.persister.database.entity.ExecutionInstanceEntity">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate"/>
        <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified"/>
        <result column="process_instance_id" jdbcType="BIGINT" property="processInstanceId"/>
        <result column="process_definition_id_and_version" jdbcType="VARCHAR" property="processDefinitionIdAndVersion"/>
        <result column="process_definition_activity_id" jdbcType="VARCHAR" property="processDefinitionActivityId"/>
        <result column="activity_instance_id" jdbcType="BIGINT" property="activityInstanceId"/>
        <result column="active" jdbcType="TINYINT" property="active"/>
    </resultMap>
    <!-- income_transition_id,income_activity_instance_id, -->
    <sql id="baseColumn">
        id, gmt_create, gmt_modified,
        process_instance_id,process_definition_id_and_version,process_definition_activity_id,activity_instance_id,active
    </sql>

    <!-- #{incomeTransitionId},#{incomeActivityInstanceId}, -->
    <insert id="insert" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.ExecutionInstanceEntity" useGeneratedKeys="true" keyProperty="id">
        insert into se_execution_instance(<include refid="baseColumn"/>)
        values (#{id}, now(), now(), #{processInstanceId}, #{processDefinitionIdAndVersion},
        #{processDefinitionActivityId}, #{activityInstanceId},#{active})
    </insert>

    <delete id="delete" parameterType="long">
        delete from  se_execution_instance where id=#{id}
    </delete>

    <update id="update" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.ExecutionInstanceEntity">
        update se_execution_instance
        <set>
            gmt_modified=now()
            <if test="active != null">,active = #{active}</if>
        </set>
        where id=#{id}
    </update>

    <select id="findOne" resultMap="BaseResultMap">
        select
        <include refid="baseColumn"/>
        from se_execution_instance where id=#{id}
    </select>

    <select id="findAllExecutionList" resultMap="BaseResultMap">
        select
        <include refid="baseColumn"/>
        from se_execution_instance where process_instance_id=#{processInstanceId}
    </select>

    <select id="findActiveExecution" resultMap="BaseResultMap">
        select
        <include refid="baseColumn"/>
        from se_execution_instance where process_instance_id=#{processInstanceId} and active = 1
    </select>

    <select id="findByActivityInstanceId" resultMap="BaseResultMap">
        select
        <include refid="baseColumn"/>
        from se_execution_instance where process_instance_id=#{processInstanceId} and activity_instance_id =
        #{activityInstanceId}
    </select>
</mapper>
