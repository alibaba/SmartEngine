<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.smart.framework.engine.persister.database.dao.TaskTransferRecordDAO">

    <sql id="baseColumn">
        id, gmt_create, gmt_modified, task_instance_id, from_user_id, to_user_id,
        transfer_reason, deadline, tenant_id
    </sql>

    <insert id="insert" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.TaskTransferRecordEntity"
            keyProperty="id">
        insert into se_task_transfer_record(<include refid="baseColumn"/>)
        values (#{id}, #{gmtCreate}, #{gmtModified}, #{taskInstanceId}, #{fromUserId},
                #{toUserId}, #{transferReason}, #{deadline}, #{tenantId})
    </insert>

    <update id="update" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.TaskTransferRecordEntity">
        update se_task_transfer_record
        <set>
            gmt_modified = CURRENT_TIMESTAMP
            <if test="transferReason != null">,transfer_reason = #{transferReason}</if>
            <if test="deadline != null">,deadline = #{deadline}</if>
        </set>
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </update>

    <select id="select" resultType="com.alibaba.smart.framework.engine.persister.database.entity.TaskTransferRecordEntity">
        select <include refid="baseColumn"/>
        from se_task_transfer_record
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </select>

    <select id="selectByTaskInstanceId" resultType="com.alibaba.smart.framework.engine.persister.database.entity.TaskTransferRecordEntity">
        select <include refid="baseColumn"/>
        from se_task_transfer_record
        where task_instance_id = #{taskInstanceId}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
        order by gmt_create desc
    </select>

    <delete id="delete">
        delete from se_task_transfer_record
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </delete>

</mapper>
