<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.smart.framework.engine.persister.database.dao.AssigneeOperationRecordDAO">

    <sql id="baseColumn">
        id, gmt_create, gmt_modified, task_instance_id, operation_type,
        operator_user_id, target_user_id, operation_reason, tenant_id
    </sql>

    <insert id="insert" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.AssigneeOperationRecordEntity"
            keyProperty="id">
        insert into se_assignee_operation_record(<include refid="baseColumn"/>)
        values (#{id}, #{gmtCreate}, #{gmtModified}, #{taskInstanceId}, #{operationType},
                #{operatorUserId}, #{targetUserId}, #{operationReason}, #{tenantId})
    </insert>

    <update id="update" parameterType="com.alibaba.smart.framework.engine.persister.database.entity.AssigneeOperationRecordEntity">
        update se_assignee_operation_record
        <set>
            gmt_modified = CURRENT_TIMESTAMP
            <if test="operationReason != null">,operation_reason = #{operationReason}</if>
        </set>
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </update>

    <select id="select" resultType="com.alibaba.smart.framework.engine.persister.database.entity.AssigneeOperationRecordEntity">
        select <include refid="baseColumn"/>
        from se_assignee_operation_record
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </select>

    <select id="selectByTaskInstanceId" resultType="com.alibaba.smart.framework.engine.persister.database.entity.AssigneeOperationRecordEntity">
        select <include refid="baseColumn"/>
        from se_assignee_operation_record
        where task_instance_id = #{taskInstanceId}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
        order by gmt_create desc
    </select>

    <delete id="delete">
        delete from se_assignee_operation_record
        where id = #{id}
        <if test="tenantId != null"> and tenant_id = #{tenantId} </if>
    </delete>

</mapper>
